let info = {
  name: "nitan",
  age: 30,
  address: "gagalphedi",
  surname: "thapa",
};

let myInfo = `my name is ${info.name} ${info.surname} i am ${info.age} i live in ${info.address}`;
console.log(myInfo);
